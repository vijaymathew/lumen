#pragma once

#include <QIcon>

// The Lumen application / window icon, rendered in code at several sizes so it
// stays crisp everywhere and needs no image assets or format plugins. A soft
// glowing orb framed by a thin ring on a violet tile — "lumen" = light.
QIcon makeAppIcon();
